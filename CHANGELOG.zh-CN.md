# 更新记录

更早版本的更新内容。最新一版见 [README.zh-CN.md](README.zh-CN.md)；[English](CHANGELOG.md)。

## v0.8.11

- **快捷提示词改为数据目录文件**：内置提示词、自定义按钮和相关开关保存在 `zotero-ai-sidebar-quick-prompts.json`，和 PDF 同目录（通常是用户主目录下的 `Zotero/`），不再作为大段内容写入 `prefs.js`。升级后第一次启动会迁移旧偏好项，**文件写成功后再清掉偏好项**。配置导入/导出和 WebDAV `state.json` 仍包含提示词库。
- **普通 PDF 可通过 MinerU 做全文翻译**：没有 arXiv LaTeX 源时，全文翻译会把 PDF 交给 MinerU 精准解析 API，再沿用现有对照阅读页。需在设置中填写 mineru.net Token。
- **按句对照**：使用 **逐段** 版式并选择 **按句换行** 时，每句译文直接排在该句原文下面；两侧切分出的句子数不一致时，译文仍作为整段显示在段落下方，不再错位配对。“阅读设置”还可以调整字号、行距、字体样式（系统 / 衬线 / 无衬线）、句末标记和换行方式。
- **原始 PDF 对照**：普通 PDF 的阅读页可以一边显示原始 PDF 页面，一边保留解析文字或译文。点段落定位 PDF 区域，点区域框定位段落，并支持翻页和放大；MinerU 已缓存的页码坐标会直接复用。关闭对照后，只有 **核对 PDF 原文** 按钮会展开原始页面区域。LaTeX 阅读流程不变。
- **WEB 论文材料跟随解析进度**：仍然优先发送缓存的 LaTeX 主文件；没有源码但 MinerU 已解析完成时改发解析出的 Markdown 全文（`full.md`），只有解析尚未完成才回退 PDF。首条消息发送的是 PDF 的对话，下一条消息会自动改发 Markdown。Markdown 为纯文本，图片只以相对路径出现、不会上传，网页模型看不到图表内容（本地全文翻译页不受影响）。
- **修复 WEB 附件上传卡住**：上传进度改为按网页实际显示的文件名匹配，之前停在“上传附件”约两分钟后报“未接收全部附件”的任务现在可以正常提交；任务失败或被取消后，输入框也不再残留“正在处理上一条请求”。
- **DeepSeek 对话被删除后可恢复**：绑定的网页对话已不存在时，插件会识别并重新创建对话，不再直接报错。
- **升级方法**：安装 0.8.11 XPI 后重启 Zotero。已有提示词编辑会保留；重启后错误控制台里不应再出现向 `extensions.zotero-ai-sidebar.quickPrompts` 写入约 10KB 的 Warning。WEB 用户保留浏览器登录状态；运行包校验值一致时直接复用。

## v0.8.10

- **选择 WEB 浏览器**：在“账号”旁的箭头菜单中选择 Chrome、Microsoft Edge，或自行添加支持 Chromium 远程调试的浏览器。直接显示检测到的程序路径，支持手动编辑、“选择程序文件…”和“重新检测”；每个浏览器配置独立保存登录状态。
- **原生下拉框修复**：选择浏览器时保持配置窗口展开，点击外部时收起；正确识别 Zotero 原生下拉选项，避免选择过程中误关闭窗口。
- **WEB 阅读路线与总览**：支持通过所选 AI 网站生成，两项任务使用独立会话，优先发送可用的 LaTeX 原文，否则发送论文 PDF，不附带聊天历史或选区上下文。API 生成流程保持原有实现。
- **LaTeX 批量翻译**：WEB 请求按批次发送多个文本块，校验返回的段落标记，响应不完整时暂停；改进中英对照阅读布局和参考文献交互。
- **对话滚动修复**：PDF 选中文字并标记颜色后，在必要刷新中保留当前对话位置；切换论文后进入对话末尾。修复共用于不同 Zotero 版本。
- **升级方法**：安装 0.8.10 XPI 后重启 Zotero。WEB 用户按账号窗口提示检查配套运行包；运行包内容未变化时复用已有包及登录数据。

## v0.8.9

- **完整显示算法**：LaTeX 算法的分段共用一个显示区域，原文和译文均保留跨段缩进，注释保留 `/* … */` 或 `//`。已有译文继续复用。阅读页是源码重建视图，不是与 PDF 逐像素一致的 LaTeX 编译器。
- **LaTeX 下载设置**：论文标题旁可选系统代理或不使用代理；端口默认跟随操作系统，也可单独覆盖，仅影响 LaTeX 下载。聊天准备阶段使用已完成的本地源码缓存或本地 PDF，不等待后台下载。
- **阅读与聊天稳定性**：修复启动时样式加载后的侧栏对齐；附带全文时仍保留可用工具；改进标题定位、流式滚动和笔记随论文切换。
- **输出预算**：新模型预设的默认输出上限改为 32768；已有预设保留原值，实际可用上限仍由服务端和模型决定。
- **升级方法**：安装新版 XPI 后重启 Zotero；WEB 用户按账号窗口提示检查或更新配套运行包。详见更新后的[全文翻译教程](docs/USAGE.zh-CN.md#211-阅读-arxiv-全文翻译)。

## v0.8.7

- **端口自动分配**：Web Agent 和专用浏览器动态选择空闲端口，减少与 MCP 等插件的端口冲突；XPI 自动读取 Agent 实际地址。
- **运行包与 XPI 配套**：Web Agent 不再单独发布版本，安装和手动更新时校验配套 ZIP；日常使用不重复计算摘要，升级保留已有登录资料。
- **新增 Z.ai 网页入口**：与 ChatGLM 分开配置；游客可文字聊天，上传附件需要登录。登录状态自动检测，无需关闭 Chrome。
- **ChatGLM 会话与回答处理**：保留浏览器会话，区分思考和最终回答，并移除固定的“风控受限”标记；实际网页验证仍按页面状态提示。
- **安装与教程更新**：修正 Windows 配置路径，完善环境检查、手动 ZIP 安装和账号状态说明，同步中英文指南及图解教程。

## v0.8.6

- **XPI 保持轻量**：每个具体版本 Release 同时提供预构建的 `zai-web-agent-runtime.zip`；插件下载并校验同版本资产，用户电脑不执行 npm。
- **Web Agent 安装可恢复**：自动下载失败时提供 Release 页面、直接下载链接和本地 ZIP 选择；大小、SHA-256、版本、协议与健康检查全部通过后才切换运行版本，失败则保留兼容旧版本。

## v0.8.2

- **Kimi 成为内置 WEB 服务**：旧版保存的 `kimi.com` 自定义配置会自动迁移；ChatGPT、DeepSeek、ChatGLM、Kimi 和第三方站点继续使用互相隔离的适配器与账号会话。
- **WEB 任务恢复与收尾更可靠**：重试会留在原网页服务，不会误走 API；已完成回答不会卡在过期的停止状态；按 `Esc` 或输入框的停止按钮即可释放被中断的任务，无需重启 Zotero。
- **网页错误会明确同步到 Zotero**：登录、额度、服务器和不支持上传等页面提示会显示为区分明确的错误回答，正常回答仍会持续增量同步。
- **输入区更紧凑且能区分模式**：WEB 模式不再挤入 API 专用控件，窄侧边栏中的底部状态仍可读，隐藏状态行也不会留下空白。

## v0.8.1

- **WEB 论文上下文更干净**：当前论文以真实 LaTeX/PDF 附件上传；单独的 arXiv 目录 TXT 只包含章节层级、编号和标题。
- **WEB 回答可转为 PDF 标注草稿**：全文重点与选区解释都能识别结构化标注块，在本地定位逐字原句，并允许预览、重新定位和显式保存到 Zotero，全程不调用模型 API。
- **API 与 WEB 控件边界更清楚**：WEB 使用网页自身的联网、模型和搜索状态以及论文附件流程，因此 API 专用的“联网”“原文”开关会禁用。Enter 与发送箭头执行同一套实时账号检查，底栏和状态文字也适配了窄侧边栏。
